interface:
  display_name: "ChatGPT PR Loop"
  short_description: "Run a SHA-verified PR review and fix loop"
  default_prompt: "Use $chatgpt-pr-loop to adopt this existing PR, verify its exact remote HEAD, and run the review-fix loop."
policy:
  allow_implicit_invocation: true
