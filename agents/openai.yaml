interface:
  display_name: "ChatGPT PR Loop"
  short_description: "Run SHA-verified PR review and fix loops"
  default_prompt: "Use $chatgpt-pr-loop to take over this PR and run the test-review-fix loop."
policy:
  allow_implicit_invocation: true
